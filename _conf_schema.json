{
    "enable_group_owner": {
        "description": "是否允许群主使用功能",
        "type": "bool",
        "default": true
    },
    "enable_group_admin": {
        "description": "是否允许管理员使用功能",
        "type": "bool",
        "default": true
    },
    "show_server_version": {
        "description": "是否在卡片上显示服务器版本",
        "type": "bool",
        "default": false
    },
    "show_server_latency": {
        "description": "是否在卡片上显示服务器延迟",
        "type": "bool",
        "default": false
    },
    "super_admins": {
        "description": "超级管理员",
        "type": "list",
        "hint": "可管理白名单。填写用户 QQ 号（或其他平台唯一ID）",
        "default": [],
        "items": {
            "type": "string",
            "description": "用户ID"
        }
    },
    "whitelist_admins": {
        "description": "白名单管理员",
        "type": "list",
        "hint": "与群主、管理员拥有同等权限。填写用户 QQ 号（或其他平台唯一ID）",
        "default": [],
        "items": {
            "type": "string",
            "description": "用户ID"
        }
    },
    "blacklist": {
        "description": "黑名单",
        "type": "list",
        "hint": "黑名单中的用户无法使用插件的所有功能。填写用户 QQ 号（或其他平台唯一ID）",
        "default": [],
        "items": {
            "type": "string",
            "description": "用户ID"
        }
    },
    "enable_mod_api": {
        "description": "是否启用模组 API 获取玩家数据",
        "hint": "若开启，会优先尝试连接服务器上的模组 HTTP 接口",
        "type": "bool",
        "default": false
    },
    "mod_api_port": {
        "description": "模组 API 监听的端口",
        "hint": "所有服务器统一使用",
        "type": "int",
        "default": 25580
    },
    "use_plugin_api": {
        "description": "是否启用服务端插件 API 数据源",
        "hint": "优先于 mcstatus 查询",
        "type": "bool",
        "default": true
    },
    "plugin_api_port": {
        "description": "服务端插件 HTTP API 监听的端口",
        "hint": "所有服务器统一使用",
        "type": "int",
        "default": 8612
    }
}